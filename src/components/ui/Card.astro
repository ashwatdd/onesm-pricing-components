---
export interface Props {
    className: string
}

const { className } = Astro.props
---
<section class:list={['border border-neutral-300 rounded-lg shadow-sm', className]}>
    <slot />
</section>
